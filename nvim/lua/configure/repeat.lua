local plugin = {}
plugin.core = {
    "tpope/vim-repeat",
   opt_marker = "vim的重复命令插件",
   opt_enable = true,

    --as = "vim-repeat",
    setup = function()  -- Specifies code to run before this plugin is loaded.

    end,

    config = function() -- Specifies code to run after this plugin is loaded

    end,

}

plugin.mapping = function()

end
return plugin
